import { Action } from 'routing-controllers';
import { Service } from "typedi";

@Service()
export class <%= name %>Guard {
  constructor(private readonly opt: string[] = []) {}

  async check(action: Action): Promise<boolean> {
    return true;
  }
}