import { JsonController, Get } from 'routing-controllers';
import { Service } from "typedi";

@Service()
@JsonController('/<%= toKebabPath(name) %>')
export class <%= name %>Controller {
  @Get()
  async get() {
    return 'Hello Word';
  }
}
