import { Action, Interceptor, InterceptorInterface } from 'routing-controllers';
import { Service } from "typedi";

@Service()
@Interceptor()
export class <%= name %>Interceptor implements InterceptorInterface {
  intercept(action: Action, result: any) {
    return result
  }
}
